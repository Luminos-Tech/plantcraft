services:
  app:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "6000:6000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SITE_URL=https://plantcraft.luminostech.tech
    command: sh -c "npm install && npm run build && npm run start"
